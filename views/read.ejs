<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <style>
      /* Base layout */
      body {
        font-family: Arial, sans-serif;
        max-width: 800px;
        margin: 50px auto;
        padding: 20px;
      }

      /* Student Info Header */
      .student-header {
        position: fixed;
        top: 0;
        right: 20px;
        background: linear-gradient(135deg, #2196f3, #1976d2);
        color: white;
        padding: 10px 20px;
        border-radius: 0 0 15px 15px;
        box-shadow: 0 4px 12px rgba(33, 150, 243, 0.3);
        z-index: 1000;
        display: flex;
        align-items: center;
        gap: 12px;
        font-size: 14px;
      }
      .student-icon {
        width: 35px;
        height: 35px;
        background: #1976d2;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
        font-size: 16px;
        border: 2px solid white;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
      }
      .student-info {
        display: flex;
        flex-direction: column;
        line-height: 1.3;
      }
      .student-id {
        font-weight: bold;
        font-size: 13px;
      }
      .student-name {
        font-size: 12px;
        opacity: 0.9;
      }

      h1 {
        color: #1976d2;
        border-bottom: 3px solid #2196f3;
        padding-bottom: 10px;
        margin-bottom: 30px;
        margin-top: 60px; /* Add space for fixed header */
      }

      /* Post content */
      .post-header {
        background-color: #f8f9fa;
        padding: 20px;
        border: 1px solid #dee2e6;
        border-radius: 4px;
        margin-bottom: 20px;
      }
      .post-title {
        font-size: 1.5em;
        font-weight: bold;
        color: #333;
        margin-bottom: 10px;
      }
      .post-meta {
        display: flex;
        justify-content: space-between;
        color: #666;
        font-size: 14px;
        border-top: 1px solid #dee2e6;
        padding-top: 10px;
      }
      .meta-item {
        display: flex;
        align-items: center;
      }
      .meta-label {
        font-weight: bold;
        margin-right: 5px;
      }
      .post-body {
        background-color: #ffffff;
        padding: 30px;
        border: 1px solid #dee2e6;
        border-radius: 4px;
        min-height: 300px;
        line-height: 1.8;
        white-space: pre-wrap;
        margin-bottom: 20px;
        font-size: 16px;
      }

      /* Image display */
      .post-image-section {
        margin-bottom: 20px;
      }
      .image-label {
        font-weight: bold;
        color: #555;
        margin-bottom: 10px;
        font-size: 14px;
      }
      .post-image {
        text-align: center;
        padding: 20px;
        border: 1px solid #dee2e6;
        border-radius: 4px;
        background-color: #ffffff;
      }
      .post-image img {
        max-width: 100%;
        max-height: 300px;
        border-radius: 4px;
      }
      .no-image {
        color: #999;
        font-style: italic;
        padding: 40px 0;
        text-align: center;
        border: 1px solid #dee2e6;
        border-radius: 4px;
        background-color: #f9f9f9;
      }

      /* Action buttons */
      .post-footer {
        border-top: 1px solid #dee2e6;
        padding-top: 20px;
        text-align: center;
      }
      .btn {
        display: inline-block;
        padding: 10px 20px;
        margin: 0 5px;
        text-decoration: none;
        border-radius: 4px;
        cursor: pointer;
        border: none;
        font-weight: normal;
      }
      .btn-update {
        background-color: #2196f3;
        color: white;
      }
      .btn-update:hover {
        background-color: #1976d2;
      }
      .btn-delete {
        background-color: #dc3545;
        color: white;
      }
      .btn-delete:hover {
        background-color: #c82333;
      }
      .btn-list {
        background-color: #6c757d;
        color: white;
      }
      .btn-list:hover {
        background-color: #5a6268;
      }

      /* Delete modal */
      .modal {
        display: none;
        position: fixed;
        z-index: 999;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
      }
      .modal-content {
        background-color: #fefefe;
        margin: 15% auto;
        padding: 20px;
        border-radius: 5px;
        width: 400px;
        max-width: 90%;
      }
      .modal-header {
        margin-bottom: 20px;
        font-size: 18px;
        font-weight: bold;
      }
      .modal-body {
        margin-bottom: 20px;
      }
      .modal-footer {
        text-align: right;
      }
      .form-group {
        margin-bottom: 15px;
      }
      label {
        display: block;
        margin-bottom: 5px;
        font-weight: bold;
      }
      input[type="password"] {
        width: 100%;
        padding: 8px;
        border: 1px solid #ddd;
        border-radius: 4px;
        box-sizing: border-box;
      }

      /* Responsive design */
      @media (max-width: 600px) {
        .post-meta {
          flex-direction: column;
          gap: 5px;
        }
        .btn {
          display: block;
          margin: 5px auto;
          width: 150px;
        }
      }
    </style>
  </head>
  <body>
    <!-- Student Info Header -->
    <div class="student-header">
      <div class="student-icon">B</div>
      <div class="student-info">
        <div class="student-id">2021202502</div>
        <div class="student-name">Tran Xuan Binh</div>
      </div>
    </div>

    <h1><%= title %></h1>

    <div class="post-header">
      <div class="post-title"><%= post.title %></div>
      <div class="post-meta">
        <div class="meta-item">
          <span class="meta-label">글번호:</span>
          <span>#<%= post.idx %></span>
        </div>
        <div class="meta-item">
          <span class="meta-label">작성자:</span>
          <span><%= post.creator_id %></span>
        </div>
        <div class="meta-item">
          <span class="meta-label">조회수:</span>
          <span><%= post.hit %></span>
        </div>
      </div>
    </div>

    <!-- 게시글 본문 -->
    <div class="post-body"><%= post.content %></div>

    <!-- 첨부 이미지 섹션 -->
    <div class="post-image-section">
      <div class="image-label">첨부 이미지:</div>
      <% if (post.image_path && post.image_path.trim() !== '') { %>
      <div class="post-image">
        <img
          src='<%= process.env.BASE_URL + "/uploads/" + post.image_path %>'
          alt="첨부 이미지"
        />
      </div>
      <% } else { %>
      <div class="no-image">첨부된 이미지가 없습니다</div>
      <% } %>
    </div>

    <!-- 게시글 푸터 -->
    <div class="post-footer">
      <form style="display: inline" action="/board/update" method="get">
        <input type="hidden" name="idx" value="<%= post.idx %>" />
        <button type="submit" class="btn btn-update">글 수정</button>
      </form>
      <button type="button" class="btn btn-delete" id="deleteBtn">
        글 삭제
      </button>
      <a href="/board/" class="btn btn-list">목록으로</a>
    </div>

    <!-- Delete Modal -->
    <div id="deleteModal" class="modal">
      <div class="modal-content">
        <div class="modal-header">게시글 삭제</div>
        <div class="modal-body">
          <form id="deleteForm" method="POST" action="/board/delete">
            <input type="hidden" name="idx" value="<%= post.idx %>" />
            <div class="form-group">
              <label>비밀번호를 입력하세요:</label>
              <input
                type="password"
                name="passwd"
                id="deletePassword"
                placeholder="비밀번호"
                required
              />
            </div>
            <p style="color: #dc3545; font-size: 14px">
              ※ 삭제된 게시글은 복구할 수 없습니다.
            </p>
          </form>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-list" id="cancelDelete">
            취소
          </button>
          <button type="button" class="btn btn-delete" id="confirmDelete">
            삭제
          </button>
        </div>
      </div>
    </div>

    <script>
      $(document).ready(function () {
        // Show delete modal
        $("#deleteBtn").click(function () {
          $("#deleteModal").show();
          $("#deletePassword").focus();
        });

        // Close modal
        $("#cancelDelete").click(function () {
          $("#deleteModal").hide();
          $("#deletePassword").val("");
        });

        // Close modal on outside click
        $(window).click(function (event) {
          if (event.target.id === "deleteModal") {
            $("#deleteModal").hide();
            $("#deletePassword").val("");
          }
        });

        // Submit delete form with confirmation
        $("#confirmDelete").click(function () {
          var password = $("#deletePassword").val().trim();
          if (password === "") {
            $("#deletePassword").focus();
            return;
          }
          if (confirm("정말로 이 게시글을 삭제하시겠습니까?")) {
            $("#deleteForm").submit();
          }
        });

        // Submit on Enter key
        $("#deletePassword").keypress(function (e) {
          if (e.which === 13) {
            $("#confirmDelete").click();
          }
        });
      });
    </script>
  </body>
</html>
