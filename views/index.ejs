<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 900px;
      margin: 50px auto;
      padding: 20px;
    }

    /* Student Info Header */
    .student-header {
      position: fixed;
      top: 0;
      right: 20px;
      background: linear-gradient(135deg, #2196F3, #1976D2);
      color: white;
      padding: 10px 20px;
      border-radius: 0 0 15px 15px;
      box-shadow: 0 4px 12px rgba(33, 150, 243, 0.3);
      z-index: 1000;
      display: flex;
      align-items: center;
      gap: 12px;
      font-size: 14px;
    }
    .student-icon {
      width: 35px;
      height: 35px;
      background: #1976D2;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      font-size: 16px;
      border: 2px solid white;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
    }
    .student-info {
      display: flex;
      flex-direction: column;
      line-height: 1.3;
    }
    .student-id {
      font-weight: bold;
      font-size: 13px;
    }
    .student-name {
      font-size: 12px;
      opacity: 0.9;
    }

    h1 {
      color: #1976D2;
      border-bottom: 3px solid #2196F3;
      padding-bottom: 10px;
      margin-top: 60px; /* Add space for fixed header */
    }
    h2 {
      color: #1976D2;
      margin-top: 30px;
      margin-bottom: 15px;
      font-size: 1.5em;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }
    th, td {
      border: 1px solid #ddd;
      padding: 12px;
      text-align: left;
    }
    th {
      background-color: #2196F3;
      color: white;
      text-align: center;
    }
    td {
      text-align: center;
    }
    .title-cell {
      text-align: left;
    }
    .title-link {
      text-decoration: none;
      color: #333;
    }
    .title-link:hover {
      color: #2196F3;
      text-decoration: underline;
    }
    .btn {
      display: inline-block;
      padding: 10px 20px;
      background-color: #2196F3;
      color: white;
      text-decoration: none;
      border: none;
      border-radius: 4px;
      margin-right: 10px;
      margin-top: 20px;
      cursor: pointer;
    }
    .btn:hover {
      background-color: #1976D2;
    }
    .btn-secondary {
      background-color: #6c757d;
    }
    .btn-secondary:hover {
      background-color: #5a6268;
    }
    .btn-container {
      text-align: center;
      margin-top: 30px;
    }
  </style>
</head>
<body>
  <!-- Student Info Header -->
  <div class="student-header">
    <div class="student-icon">B</div>
    <div class="student-info">
      <div class="student-id">2021202502</div>
      <div class="student-name">Tran Xuan Binh</div>
    </div>
  </div>

  <h1><%= title %></h1>
  
  <h2>최근 게시글</h2>
  
  <table>
    <tr>
      <th width="10%">번호</th>
      <th width="15%">작성자</th>
      <th width="50%">제목</th>
      <th width="10%">조회수</th>
    </tr>
    <% if (rows && rows.length > 0) { %>
      <% for(var i=0; i<rows.length; i++) { %>
      <tr>
        <td><%= rows[i].idx %></td>
        <td><%= rows[i].creator_id %></td>
        <td class="title-cell">
          <a href="/board/read/<%= rows[i].idx %>" class="title-link">
            <%= rows[i].title %>
          </a>
        </td>
        <td><%= rows[i].hit %></td>
      </tr>
      <% } %>
    <% } else { %>
      <tr>
        <td colspan="4" style="text-align: center; padding: 40px; color: #999;">
          등록된 게시글이 없습니다.
        </td>
      </tr>
    <% } %>
  </table>
  
  <p>Total: <%= rows ? rows.length : 0 %> posts</p>
  
  <div class="btn-container">
    <button id="writeBtn" class="btn">글쓰기</button>
    <button id="boardListBtn" class="btn btn-secondary">게시판 목록</button>
  </div>

  <script>
    $(document).ready(function() {
      // redirect to write page
      $('#writeBtn').click(function(event) {
        event.preventDefault();
        window.location.href = '/board/write';
      });
      
      // redirect to board list page
      $('#boardListBtn').click(function(event) {
        event.preventDefault();
        window.location.href = '/board';
      });
    });
  </script>
</body>
</html>