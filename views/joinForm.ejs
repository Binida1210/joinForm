<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <style>
      body {
        font-family: Arial, sans-serif;
        max-width: 600px;
        margin: 50px auto;
        padding: 20px;
      }
      .form-group {
        margin-bottom: 15px;
      }
      label {
        display: block;
        margin-bottom: 5px;
        font-weight: bold;
      }
      input[type="text"],
      input[type="password"],
      input[type="email"],
      input[type="tel"],
      input[type="date"],
      select {
        width: 100%;
        padding: 8px;
        border: 1px solid #ddd;
        border-radius: 4px;
        box-sizing: border-box;
      }
      input[type="radio"] {
        margin-right: 5px;
      }
      button {
        background-color: #2196f3;
        color: white;
        padding: 10px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 16px;
      }
      button:hover {
        background-color: #1976d2;
      }
    </style>
  </head>
  <body>
    <h1><%= title %></h1>

    <form id="joinForm" method="post" action="/joinForm" novalidate>
      <div class="form-group">
        <label>아이디 (ID):</label>
        <input type="text" name="userId" id="userId" required />
      </div>

      <div class="form-group">
        <label>비밀번호 (Password):</label>
        <input type="password" name="userPw" id="userPw" required />
      </div>

      <div class="form-group">
        <label>이름 (Name):</label>
        <input type="text" name="userName" id="userName" required />
      </div>

      <div class="form-group">
        <label>이메일 (Email):</label>
        <input type="email" name="userEmail" id="userEmail" required />
      </div>

      <div class="form-group">
        <label>전화번호 (Phone):</label>
        <input type="tel" name="userPhone" id="userPhone" required />
      </div>

      <div class="form-group">
        <label>주소 (Address):</label>
        <input type="text" name="userAddr" id="userAddr" required />
      </div>

      <div class="form-group">
        <label>직업 (Job):</label>
        <select name="userJob" id="userJob" required>
          <option value="">선택하세요</option>
          <option value="학생">학생</option>
          <option value="회사원">회사원</option>
          <option value="프리랜서">프리랜서</option>
          <option value="기타">기타</option>
        </select>
      </div>

      <div class="form-group">
        <label>성별 (Gender):</label>
        <input type="radio" name="userGender" value="남성" required /> 남성
        <input type="radio" name="userGender" value="여성" /> 여성
      </div>

      <div class="form-group">
        <label>생일 (Birthday):</label>
        <input type="date" name="userBirth" id="userBirth" required />
      </div>

      <button type="submit">가입하기</button>
    </form>

    <script>
      $(document).ready(function () {
        $("#joinForm").submit(function (e) {
          // setup form data
          var formData = {
            userId: $("#userId").val().trim(),
            userPw: $("#userPw").val().trim(),
            userName: $("#userName").val().trim(),
            userEmail: $("#userEmail").val().trim(),
            userPhone: $("#userPhone").val().trim(),
            userAddr: $("#userAddr").val().trim(),
            userJob: $("#userJob").val(),
            userGender: $('input[name="userGender"]:checked').val(),
            userBirth: $("#userBirth").val(),
          };

          // validate all fields are filled
          for (var key in formData) {
            if (!formData[key]) {
              alert("모든 항목을 입력해주세요!");
              return false;
            }
          }

          // submit form via AJAX
          $.ajax({
            type: "POST",
            url: "/joinForm",
            data: formData,
            success: function (response) {
              $("#resultData").text(JSON.stringify(response, null, 2));
              $("#result").slideDown();
              console.log("Server response:", response);
            },
            error: function (error) {
              alert("오류가 발생했습니다!");
              console.error(error);
            },
          });
        });
      });
    </script>
  </body>
</html>
